# WeatherZen
Go to https://openweathermap.org/, generate your api key and insert it in variable called API_KEY located in index.js. <br>
Type npm i in terminal to install the package. <br>
Type nodemon index.js in terminal to run the app.
